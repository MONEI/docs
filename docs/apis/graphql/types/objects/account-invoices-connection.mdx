---
id: account-invoices-connection
title: AccountInvoicesConnection
---

export const Bullet = () => (
  <>
    <span
      style={{fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)'}}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{fontSize: '1.5em', paddingLeft: '4px'}}
      target="_blank"
      href={props.url}
      title={'Specified by ' + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import {useState} from 'react';

export const Details = ({dataOpen, dataClose, children, startOpen = false}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? {open: true} : {})}
      className="details"
      style={{border: 'none', boxShadow: 'none', background: 'var(--ifm-background-color)'}}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{listStyle: 'none'}}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Account invoices connection

```graphql
type AccountInvoicesConnection {
  items: [AccountInvoice!]!
  total: Int!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>AccountInvoicesConnection.<b>items</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>[AccountInvoice!]!</b></code>](/docs/apis/graphql/types/objects/account-invoice.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="object"/>

List of account invoices

#### [<code style={{ fontWeight: 'normal' }}>AccountInvoicesConnection.<b>total</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int!</b></code>](/docs/apis/graphql/types/scalars/int.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

Total number of account invoices

### Returned By

[`accountInvoices`](/docs/apis/graphql/operations/queries/account-invoices.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
