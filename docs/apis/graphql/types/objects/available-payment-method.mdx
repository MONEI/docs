---
id: available-payment-method
title: AvailablePaymentMethod
---

export const Bullet = () => (
  <>
    <span
      style={{fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)'}}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{fontSize: '1.5em', paddingLeft: '4px'}}
      target="_blank"
      href={props.url}
      title={'Specified by ' + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import {useState} from 'react';

export const Details = ({dataOpen, dataClose, children, startOpen = false}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? {open: true} : {})}
      className="details"
      style={{border: 'none', boxShadow: 'none', background: 'var(--ifm-background-color)'}}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{listStyle: 'none'}}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
type AvailablePaymentMethod {
  paymentMethod: PaymentMethods
  configured: Boolean
  enabled: Boolean
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>AvailablePaymentMethod.<b>paymentMethod</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>PaymentMethods</b></code>](/docs/apis/graphql/types/enums/payment-methods.mdx) <Badge class="badge badge--secondary " text="enum"/>

The specific payment method

#### [<code style={{ fontWeight: 'normal' }}>AvailablePaymentMethod.<b>configured</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Boolean</b></code>](/docs/apis/graphql/types/scalars/boolean.mdx) <Badge class="badge badge--secondary " text="scalar"/>

Whether there is one provider configured for the payment method

#### [<code style={{ fontWeight: 'normal' }}>AvailablePaymentMethod.<b>enabled</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Boolean</b></code>](/docs/apis/graphql/types/scalars/boolean.mdx) <Badge class="badge badge--secondary " text="scalar"/>

Whether there is one provider enabled for the payment method

### Returned By

[`availablePaymentMethods`](/docs/apis/graphql/operations/queries/available-payment-methods.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
