---
id: add-blacklist-item
title: addBlacklistItem
---

export const Bullet = () => (
  <>
    <span
      style={{fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)'}}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{fontSize: '1.5em', paddingLeft: '4px'}}
      target="_blank"
      href={props.url}
      title={'Specified by ' + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import {useState} from 'react';

export const Details = ({dataOpen, dataClose, children, startOpen = false}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? {open: true} : {})}
      className="details"
      style={{border: 'none', boxShadow: 'none', background: 'var(--ifm-background-color)'}}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{listStyle: 'none'}}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Add a new item to the blacklist

```graphql
addBlacklistItem(
  input: BlacklistItemInput!
): BlacklistItem
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>addBlacklistItem.<b>input</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>BlacklistItemInput!</b></code>](/docs/apis/graphql/types/inputs/blacklist-item-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/>

### Type

#### [<code style={{ fontWeight: 'normal' }}><b>BlacklistItem</b></code>](/docs/apis/graphql/types/objects/blacklist-item.mdx) <Badge class="badge badge--secondary " text="object"/>

Blacklist item, e.g. email, phone, ip, card, bin, fingerprint.
All payments with this value will be blocked.
