---
id: devicemodel
title: 'DeviceModel'
description: 'Information about the device used for the browser session (e.g., `iPhone`).'
sidebar_label: 'DeviceModel'
hide_title: true
hide_table_of_contents: true
schema: true
sample: null
custom_edit_url: null
---

import Schema from '@theme/Schema';
import Heading from '@theme/Heading';

<Heading as={'h1'} className={'openapi__heading'} children={'DeviceModel'}></Heading>

Information about the device used for the browser session (e.g., `iPhone`).

<Schema
  schema={{
    type: 'string',
    example: null,
    description: 'Information about the device used for the browser session (e.g., `iPhone`).',
    title: 'DeviceModel'
  }}
  schemaType={'response'}
></Schema>
